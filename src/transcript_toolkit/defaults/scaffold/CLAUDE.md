Read `AGENTS.md` in this directory before doing anything in this workspace, and follow its
operating rules — especially: never pass `--yes`/`--skip-demo-check`, and get explicit user
confirmation before any full (non-demo) run.
